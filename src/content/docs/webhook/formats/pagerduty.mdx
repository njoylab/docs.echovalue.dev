---
title: "Webhook Format: PagerDuty"
description: Forward inbound email to PagerDuty using echoValue to trigger incidents automatically
sidebar:
  order: 5
---

import { Steps } from '@astrojs/starlight/components';

When `format` is set to `pagerduty`, incoming emails trigger PagerDuty incidents via the [Events API v2](https://developer.pagerduty.com/docs/ZG9jOjExMDI5NTgw-events-api-v2-overview). This format requires a `routing_key` option.

:::note
This format is available only for inbound email webhooks. Scheduled webhooks always send the scheduled-run JSON payload and do not use format adapters.
:::

## Get the Routing Key

<Steps>

1. In PagerDuty, go to **Services** and select the service you want to trigger.
2. Open **Integrations** and add **Events API v2**.
3. Copy the integration key.

</Steps>

## Configure in echoValue

```sh
curl 'https://api.echovalue.dev/webhook' \
  -H 'x-token: mytoken' \
  -H 'Content-Type: application/json' \
  -d '{
    "url": "https://events.pagerduty.com/v2/enqueue",
    "format": "pagerduty",
    "options": {
      "routing_key": "a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6"
    }
  }'
```

## Output Mapping

| PagerDuty field | Value |
| --------------- | ----- |
| `summary` | Email subject |
| `source` | Sender email address |
| `severity` | `info` |
| `custom_details.text` | Plain text email body |
| `custom_details.from_name` | Sender display name |

:::note
Incidents are always created with severity `info`.
:::
